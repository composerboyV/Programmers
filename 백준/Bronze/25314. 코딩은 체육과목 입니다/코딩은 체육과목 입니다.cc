#include <iostream>

using namespace std;

int   main()
{
      int count;
      cin>>count;
      for (int j = 0; j < count; j+=4) {
            cout<<"long ";
      }
      cout<<"int"<<endl;
      return (0);
}